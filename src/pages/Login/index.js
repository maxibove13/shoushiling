//Components
import { Button } from "../../components";

// Assets
import "./styles.scss";

const Login = () => {
  const fetchEvents = async () => {
    const res = await fetch("");
  };

  return (
    <div>
      <h2>Shoushiling</h2>
      <h3>Hello World</h3>
      <Button />
    </div>
  );
};

export default Login;
